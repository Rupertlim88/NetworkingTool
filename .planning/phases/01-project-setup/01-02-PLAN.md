---
phase: 01-project-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - components/StarLevelUpAnimation/index.tsx
  - components/StarLevelUpAnimation/Star.tsx
  - components/StarLevelUpAnimation/types.ts
  - components/StarLevelUpAnimation/utils.ts
  - lib/constants.ts
  - app/dev/page.tsx
autonomous: false
requirements:
  - INT-01
  - INT-02
  - INT-03

must_haves:
  truths:
    - "The component folder exists at components/StarLevelUpAnimation/ with exactly four files"
    - "Importing { StarLevelUpAnimation } from '@/components/StarLevelUpAnimation' compiles without errors"
    - "The /dev route renders a placeholder contact card and a 'Level Up' button"
    - "No file in components/StarLevelUpAnimation/ imports from app/, store/, or any app-level data layer"
    - "No any types exist in any file under components/StarLevelUpAnimation/"
    - "lib/constants.ts exports STAR_COLORS with yellow and blue hex values"
  artifacts:
    - path: "components/StarLevelUpAnimation/index.tsx"
      provides: "Main component — 'use client', registers useGSAP, exports StarLevelUpAnimation named export"
      exports: ["StarLevelUpAnimation"]
    - path: "components/StarLevelUpAnimation/Star.tsx"
      provides: "Individual star element stub — 'use client', typed props, renders null"
    - path: "components/StarLevelUpAnimation/types.ts"
      provides: "StarColor type alias and StarLevelUpAnimationProps interface"
      exports: ["StarColor", "StarLevelUpAnimationProps"]
    - path: "components/StarLevelUpAnimation/utils.ts"
      provides: "Animation helpers placeholder — empty exports, no implementation yet"
    - path: "lib/constants.ts"
      provides: "Star semantic color constants — STAR_COLORS with yellow and blue hex values"
      exports: ["STAR_COLORS"]
    - path: "app/dev/page.tsx"
      provides: "Dev harness at /dev route — mock contact card + Level Up button + StarLevelUpAnimation stub"
  key_links:
    - from: "app/dev/page.tsx"
      to: "components/StarLevelUpAnimation/index.tsx"
      via: "import { StarLevelUpAnimation } from '@/components/StarLevelUpAnimation'"
      pattern: "@/components/StarLevelUpAnimation"
    - from: "components/StarLevelUpAnimation/index.tsx"
      to: "components/StarLevelUpAnimation/types.ts"
      via: "import type { StarLevelUpAnimationProps } from './types'"
      pattern: "from './types'"
    - from: "components/StarLevelUpAnimation/Star.tsx"
      to: "components/StarLevelUpAnimation/types.ts"
      via: "import type { StarColor } from './types'"
      pattern: "from './types'"
---

<objective>
Create the self-contained `components/StarLevelUpAnimation/` folder with four stub files, the `lib/constants.ts` file with star semantic colors, and the `/dev` route dev harness. This establishes the component's public API surface and import path before any animation logic is written.

Purpose: Phase 2 (animation build) needs a typed, importable component folder, locked color constants, and a working dev harness to test against. This plan creates all three.
Output: Four stub files in the component folder, star color constants in lib/, one dev route file — all compiling cleanly with zero `any` types.
</objective>

<execution_context>
@/Users/rupertlim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rupertlim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-setup/01-CONTEXT.md
@.planning/phases/01-project-setup/01-RESEARCH.md
@.planning/phases/01-project-setup/01-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan must produce. Executor writes these files. -->

components/StarLevelUpAnimation/types.ts must export:
```typescript
export type StarColor = 'yellow' | 'blue';

export interface StarLevelUpAnimationProps {
  fromStars: 1 | 2;
  toStars: 2 | 3;
  contactPhoto: string;
  contactName: string;
  starColor: StarColor;
  onComplete: () => void;
}
```

components/StarLevelUpAnimation/index.tsx must export:
```typescript
export { StarLevelUpAnimation } from './StarLevelUpAnimation';
// OR implement directly and export as named export — either barrel or direct
export function StarLevelUpAnimation(_props: StarLevelUpAnimationProps): React.JSX.Element
```

lib/constants.ts must export:
```typescript
export const STAR_COLORS = {
  yellow: '#f59e0b',
  blue: '#22d3ee',
} as const
```

app/dev/page.tsx receives hardcoded props:
  fromStars={1}
  toStars={2}
  starColor="yellow"
  contactName="Rupert Lim"
  contactPhoto=""
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/constants.ts and component folder with four stub files</name>
  <files>
    lib/constants.ts,
    components/StarLevelUpAnimation/types.ts,
    components/StarLevelUpAnimation/utils.ts,
    components/StarLevelUpAnimation/Star.tsx,
    components/StarLevelUpAnimation/index.tsx
  </files>
  <action>
Create `lib/constants.ts` at the project root (parallel to `app/` and `components/`):

**lib/constants.ts**
Plain TypeScript — no 'use client' directive. Export the star semantic color constants. These are fixed semantic values that NEVER change with theme. Exact values locked by project decision:
```typescript
export const STAR_COLORS = {
  yellow: '#f59e0b',  // recently talked to this person
  blue: '#22d3ee',    // produced a real outcome (referral, job, intro)
} as const
```
No other exports. No `any`.

---

Create the directory `components/StarLevelUpAnimation/` at the project root (parallel to `app/`, NOT inside `app/`).

Create the four files in this exact order (types first, so downstream files can import from it):

**1. components/StarLevelUpAnimation/types.ts**
Plain TypeScript — no 'use client' directive (no React, no DOM). Export two things:
- `StarColor` type alias: `'yellow' | 'blue'`
- `StarLevelUpAnimationProps` interface with fields: `fromStars: 1 | 2`, `toStars: 2 | 3`, `contactPhoto: string`, `contactName: string`, `starColor: StarColor`, `onComplete: () => void`
No `any`, no imports.

**2. components/StarLevelUpAnimation/utils.ts**
Plain TypeScript — no 'use client' directive. This is a stub for future animation helpers (easing configs, trajectory math). Keep it minimal. No `any`.

Example:
```typescript
// Animation helpers — populated in Phase 3+
export {};
```

**3. components/StarLevelUpAnimation/Star.tsx**
'use client' directive as FIRST LINE. Import React. Import `StarColor` type from `./types`. Define a `StarProps` interface: `{ color: StarColor }`. Export a named function component `Star` that accepts `StarProps` and returns `null` (stub — renders nothing in Phase 1). No `any`, no GSAP imports yet (that comes in Phase 3+).

**4. components/StarLevelUpAnimation/index.tsx**
'use client' directive as FIRST LINE. Import `useRef` from react. Import `useGSAP` from `@gsap/react`. Import `gsap` from `gsap`. Import `StarLevelUpAnimationProps` type from `./types`.

At MODULE LEVEL (after imports, before the component function), register the plugin:
```typescript
gsap.registerPlugin(useGSAP);
```

Export a named function component `StarLevelUpAnimation` that accepts `_props: StarLevelUpAnimationProps` (underscore prefix since props are unused in Phase 1). Inside the component:
- Create a `containerRef` with `useRef<HTMLDivElement>(null)`
- Call `useGSAP(() => { /* animation logic goes here in Phase 2+ */ }, { scope: containerRef })`
- Return a `<div ref={containerRef} />` (empty div, renders nothing visible)

CRITICAL:
- `'use client'` must be the absolute first line of index.tsx and Star.tsx
- `gsap.registerPlugin(useGSAP)` must be at MODULE LEVEL, not inside the component or the useGSAP callback
- Named export only — do NOT use `export default`
- No `any` anywhere
- components/StarLevelUpAnimation/ files must NOT import from app/, store/, or any app-level data layer. Importing from lib/constants.ts is permitted (it is a shared constants file, not a data layer).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
All four files exist in `components/StarLevelUpAnimation/`. `lib/constants.ts` exists with STAR_COLORS export. `npx tsc --noEmit` exits 0. No `any` types in any of the files. `index.tsx` and `Star.tsx` each start with `'use client'` as first line. `gsap.registerPlugin(useGSAP)` appears at module level in `index.tsx`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /dev harness route and verify full import chain</name>
  <files>app/dev/page.tsx</files>
  <action>
Create `app/dev/` directory and `app/dev/page.tsx`.

'use client' directive as FIRST LINE (this page uses useState for the trigger button).

Implement the dev harness page:
- Import `useState` from react
- Import `StarLevelUpAnimation` from `@/components/StarLevelUpAnimation`
- Local state: `const [isAnimating, setIsAnimating] = useState(false)`
- Render a dark grey full-screen background (`min-h-screen bg-gray-900`)
- Center a mock contact card using flexbox (`flex items-center justify-center`)
- Mock contact card: a grey portrait box (`w-32 h-[170px] bg-gray-600 rounded-2xl`) with "RL" initials centered inside (white text, `text-2xl font-bold`) — portrait 3:4 ratio, rounded-2xl per locked decision
- Below the avatar box, a small card area (`mt-2 p-4 bg-gray-800 rounded`) containing:
  - Contact name text: "Rupert Lim" (`text-white text-sm mb-2`)
  - "Level Up" button (`bg-yellow-500 text-black px-4 py-2 rounded font-bold`) that calls `setIsAnimating(true)` on click
- Conditionally render `StarLevelUpAnimation` when `isAnimating` is true, passing hardcoded props:
  - `fromStars={1}`
  - `toStars={2}`
  - `starColor="yellow"`
  - `contactName="Rupert Lim"`
  - `contactPhoto=""`
  - `onComplete={() => setIsAnimating(false)}`

CRITICAL:
- Import path must be `@/components/StarLevelUpAnimation` (the barrel import, not a deep path)
- No prop controls panel — keep it minimal, this is a dev tool
- No `any` types
- 'use client' must be the first line
- Card shape is portrait 3:4 with rounded-2xl (not square, not rounded-lg) — this matches the real contact card that Phase 4 animation will launch stars from

After creating the file, run the full verification sequence:
1. `npx tsc --noEmit` — must exit 0
2. `npm run build` — must exit 0
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
`app/dev/page.tsx` exists. `npm run build` exits 0. `npx tsc --noEmit` exits 0. The import `{ StarLevelUpAnimation } from '@/components/StarLevelUpAnimation'` resolves without errors. No `any` types anywhere in the project.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verify /dev route in browser</name>
  <action>Run `npm run dev` and verify the /dev route renders correctly in a browser. This is a visual and functional check that automated build passes cannot catch — confirms GSAP loads without SSR errors at runtime and the mock UI renders as expected.</action>
  <verify>
    <automated>MISSING — visual browser check required; automated build (Task 2) already passed</automated>
  </verify>
  <done>User confirms: /dev route loads without console errors, portrait card with "RL" initials and yellow "Level Up" button are visible, clicking the button produces no runtime errors.</done>
  <what-built>
    lib/constants.ts with star color constants, four component stub files (types.ts, utils.ts, Star.tsx, index.tsx) in components/StarLevelUpAnimation/, plus the /dev harness route at app/dev/page.tsx. The component is importable as a single unit and the dev harness renders a mock contact card with a Level Up button.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root
    2. Open http://localhost:3000 — should show the default Next.js home page (no errors)
    3. Navigate to http://localhost:3000/dev — should show a dark grey page with a grey portrait card (taller than wide, rounded-2xl corners) showing "RL" initials, contact name "Rupert Lim", and a yellow "Level Up" button
    4. Click "Level Up" — the button click should not throw any errors in the browser console (the component renders an empty div, so nothing visible happens, but no runtime errors)
    5. Check the browser console — no errors, no GSAP warnings about missing 'use client' or SSR issues
    6. Confirm no "window is not defined" or hydration errors appear
  </how-to-verify>
  <resume-signal>Type "approved" if the /dev route loads correctly with no console errors, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
- `ls components/StarLevelUpAnimation/` shows exactly: index.tsx, Star.tsx, types.ts, utils.ts
- `cat lib/constants.ts` shows STAR_COLORS with yellow '#f59e0b' and blue '#22d3ee'
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- http://localhost:3000/dev renders mock portrait contact card + Level Up button without console errors
- No imports in components/StarLevelUpAnimation/ reference app/, store/, or any app-level path
- Grep for `any` in the component folder returns zero results
</verification>

<success_criteria>
1. Component folder exists with 4 files, all compiling cleanly
2. `import { StarLevelUpAnimation } from '@/components/StarLevelUpAnimation'` resolves in TypeScript
3. /dev route renders and the Level Up button can be clicked without runtime errors
4. Zero `any` types anywhere in the project
5. `npm run build` exits 0 (full production build succeeds)
6. `lib/constants.ts` exports STAR_COLORS with the two locked hex values
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md` using the summary template.
</output>
